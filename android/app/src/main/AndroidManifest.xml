<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        TRANSMIT_IR is required for ConsumerIrManager to work on Android (API 19+).
        It is a NORMAL permission — auto-granted, no user dialog needed.
        MIUI on Xiaomi/Redmi specifically checks for this permission.
        Without it, MIUI throws SecurityException and blocks all IR transmissions.
    -->
    <uses-permission android:name="android.permission.TRANSMIT_IR" />

    <!-- Declare IR hardware feature (required=false so app still installs on non-IR phones) -->
    <uses-feature android:name="android.hardware.consumerir" android:required="false" />

    <application
        android:label="Daikin AC"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data android:name="flutterEmbedding" android:value="2" />
    </application>
</manifest>
